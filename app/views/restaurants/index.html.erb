<h1>Yelp MVP!</h1>
<h2>These are the restaurants available: </h2>
<% @restaurants.each do |restaurant| %>
  <div>
    <p><%= link_to "#{restaurant.name}", restaurant_path(restaurant), class: "btn btn-primary" %></p>
  </div>
<% end %>
